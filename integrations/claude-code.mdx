---
title: Claude Code
description: Connect Nitrosend to Claude Code via MCP for terminal-based AI email marketing
---

# Claude Code + Nitrosend MCP

[Claude Code](https://docs.anthropic.com/en/docs/claude-code) is Anthropic's CLI tool for working with Claude in your terminal. Connect Nitrosend via MCP and Claude Code becomes a full email marketing command center.

## What is MCP?

**MCP (Model Context Protocol)** is an open standard that lets AI tools connect to external services. When you add Nitrosend as an MCP server to Claude Code, Claude gets native access to 16 email marketing tools — no API wrappers, no middleware, no boilerplate.

## Setup

### Step 1: Get your API key

Go to **Settings > API Keys** in the [Nitrosend dashboard](https://app.nitrosend.com) and copy your key.

### Step 2: Add the MCP server

Run this single command in your terminal:

```bash
claude mcp add nitro \
  -e NITROSEND_API_KEY=nskey_live_your_key_here \
  -- npx -y @nitrosend/mcp
```

That's it. Claude Code now has full access to your email marketing.

### Step 3: Verify the connection

Start a Claude Code session and ask:

```
What's my Nitrosend account status?
```

Claude will call the `nitro_get_status` tool and show your account health, onboarding progress, and recommendations.

## How MCP works

The MCP server (`@nitrosend/mcp`) runs as a local bridge process:

1. Claude Code sends tool calls via MCP protocol (stdio)
2. The bridge forwards them as authenticated HTTP requests to `api.nitrosend.com/mcp`
3. Structured results come back to Claude with full context

No data is stored locally. All state lives in your Nitrosend account.

## MCP tools available

Once connected, Claude Code has access to these tools:

| Tool | What it does |
|------|-------------|
| `nitro_get_status` | Account health, onboarding, and recommendations |
| `nitro_compose_email` | Create, update, or clone email templates |
| `nitro_compose_campaign` | Create email or SMS campaigns with audience targeting |
| `nitro_compose_flow` | Build automation flows with triggers, steps, and branches |
| `nitro_send_message` | Send a single transactional email or SMS immediately |
| `nitro_manage_audience` | Create contacts, manage lists, record events, tag contacts |
| `nitro_import_contacts` | Bulk import contacts (up to 100 inline) |
| `nitro_search_contacts` | Search contacts by email, name, or phone |
| `nitro_query` | Query campaigns, flows, templates, segments, messages |
| `nitro_define_segment` | Create dynamic segments with filters |
| `nitro_set_brand` | Set up brand identity — colors, fonts, logo, voice |
| `nitro_set_memory` | Read and update your AI memory document |
| `nitro_manage_domains` | Add, verify, and manage sending domains |
| `nitro_control_delivery` | Approve, send, pause, or schedule campaigns and flows |
| `nitro_preview_and_test` | Preview emails and send test emails |
| `nitro_get_insights` | Email analytics with trends and benchmarks |

## MCP resources

Claude can also read live resources for deeper context:

| Resource | What it provides |
|----------|-----------------|
| `nitro://guide` | Platform vocabulary, workflows, and tool descriptions |
| `nitro://schema` | Email component types, flow step types, filter definitions |
| `nitro://brand` | Your brand identity — colors, fonts, tone, logo |
| `nitro://account/state` | Current account status, tier, and limits |
| `nitro://account/config` | Sender defaults and verified domains |

## Example workflows

### Create a welcome flow

```
Create a welcome email flow. When a new contact is added, send a welcome 
email immediately, wait 3 days, then send a follow-up with our top 
recommendations.
```

### Send a campaign

```
Create a campaign called "March Newsletter" and send it to my subscriber list. 
Use my brand colors and include a section about our new features.
```

### Check performance

```
Show me insights for my last campaign. How did it compare to benchmarks?
```

### Set up brand and memory

```
Set up my brand from https://mysite.com and then update my AI memory with 
our Q1 marketing goals.
```

## Scope-based configuration

Add the MCP server at different scopes depending on your needs:

```bash
# User scope — available in all Claude Code sessions (recommended)
claude mcp add nitro -s user \
  -e NITROSEND_API_KEY=nskey_live_... \
  -- npx -y @nitrosend/mcp

# Project scope — only available in this directory
claude mcp add nitro -s project \
  -e NITROSEND_API_KEY=nskey_live_... \
  -- npx -y @nitrosend/mcp
```

## Troubleshooting

<AccordionGroup>
  <Accordion title="MCP server not found">
    Make sure you have Node.js 18+ installed and `npx` is on your PATH.
  </Accordion>
  <Accordion title="Authentication failed">
    Double-check your API key. It should start with `nskey_live_` for production or `nskey_test_` for test mode.
  </Accordion>
  <Accordion title="Tool call timed out">
    The MCP server includes automatic retry logic for transient errors. If calls consistently time out, check your network connection and that `api.nitrosend.com` is reachable.
  </Accordion>
</AccordionGroup>

<Info>
  The MCP package is `@nitrosend/mcp` on npm. No global install needed — `npx -y` handles everything.
</Info>
